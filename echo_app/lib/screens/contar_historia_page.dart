import 'package:flutter/material.dart';

class ContarHistoriaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Escuchar Historia'),
      ),
      body: Center(
        child: Text('Página para escuchar historia'),
      ),
    );
  }
}
