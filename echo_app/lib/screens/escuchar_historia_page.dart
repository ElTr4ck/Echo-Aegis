import 'package:flutter/material.dart';

class EscucharHistoriaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Contar Historia'),
      ),
      body: Center(
        child: Text('Página para contar historia'),
      ),
    );
  }
}